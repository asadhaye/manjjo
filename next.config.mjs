/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Enable image optimization in production; keep disabled in dev if necessary
    unoptimized: false,
  },
  
}

export default nextConfig
